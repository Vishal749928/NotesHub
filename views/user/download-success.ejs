<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Download Ready</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
/* --- SAME CSS AS YOU PROVIDED (UNCHANGED) --- */
:root {
  --green: #16a34a;
  --green-dark: #15803d;
  --red: #dc2626;
  --red-dark: #b91c1c;
  --bg: #f8fafc;
  --card: #ffffff;
  --text: #0f172a;
  --muted: #64748b;
}
body {
  margin: 0;
  font-family: "Segoe UI", system-ui, sans-serif;
  background: linear-gradient(135deg, #ecfeff, #f0fdf4);
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}
.card {
  background: var(--card);
  width: 92%;
  max-width: 420px;
  padding: 32px 26px;
  border-radius: 22px;
  box-shadow: 0 20px 50px rgba(0,0,0,0.15);
  text-align: center;
}
.check {
  width: 80px;
  height: 80px;
  background: radial-gradient(circle at top left, #4ade80, #16a34a);
  color: white;
  border-radius: 50%;
  font-size: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto 18px;
}
.actions {
  margin-top: 26px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.btn {
  padding: 15px;
  border-radius: 30px;
  font-size: 17px;
  font-weight: 700;
  border: none;
  cursor: pointer;
}
.download-btn {
  background: linear-gradient(135deg, #22c55e, #16a34a);
  color: white;
}
.back-btn {
  background: linear-gradient(135deg, #ef4444, #dc2626);
  color: white;
}
</style>
</head>

<body>

<div class="card">

  <div class="check">✔</div>
  <h2>Payment Successful</h2>
  <p>Your PDF is ready to download</p>

  <div class="actions">

    <!-- DOWNLOAD -->
    <a href="/download/file?token=<%= token %>" class="btn download-btn">
      ⬇️ Download PDF
    </a>

    <!-- BACK -->
   <button class="btn back-btn" onclick="goBackHome()">
  ⬅️ Back to Home
</button>


  </div>

</div>

<script>
function goBackHome() {

  const ok = confirm(
    "⚠️ Important Notice\n\nAfter going back, you will NOT be able to download this PDF again.\n\nContinue?"
  );

  if (!ok) return;

  fetch("/download/expire-token", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      token: "<%= token %>"
    })
  })
  .finally(() => {
    // Replace history so user can't go back
    window.location.replace("/computer");
  });
}
</script>


</body>
</html>
